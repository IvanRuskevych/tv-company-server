const { loginSchema } = require('./authSchema');
const { showSchema } = require('./showSchema');
const { customerSchema } = require('./customerSchema');

module.exports = { loginSchema, showSchema, customerSchema };
